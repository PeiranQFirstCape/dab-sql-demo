# This is a Databricks asset bundle definition for dab-sql.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab-sql
  uuid: eb70367c-9013-47e3-8362-ba09c630cb20

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  warehouse_id:
    description: The warehouse to use
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1479899030911672.12.azuredatabricks.net
    variables:
      warehouse_id: ba0be8c90f604c34
      catalog: wealth_dbt_sandbox
      schema: peiran

  # prod:
  #   mode: production
  #   workspace:
  #     host: https://adb-1479899030911672.12.azuredatabricks.net
  #     # We explicitly deploy to /Workspace/Users/peiran.quan@firstcape.co.nz to make sure we only have a single copy.
  #     root_path: /Workspace/Users/peiran.quan@firstcape.co.nz/.bundle/${bundle.name}/${bundle.target}
  #   variables:
  #     warehouse_id: ba0be8c90f604c34
  #     catalog: wealth_dbt_sandbox
  #     schema: default
  #   permissions:
  #     - user_name: peiran.quan@firstcape.co.nz
  #       level: CAN_MANAGE
